# AGENTS.md

---

## 项目概述

**HNRP** 是一个基于Unity 2022.3 版本的自定义渲染管线项目，包含 ShaderLibrary。

- **仓库**: `HaoNaN9279/HNRP`
- **目标平台**: Unity 2022.3
- **语言**: C# (.NET Standard 2.1)、hlsl

---

## 架构

- 见架构文档：架构~/最终架构.md

---

## 开发流程

### 代码变更流程

1. 从 `main` 分支创建功能分支
2. 编写代码，包含完整 XML 文档注释
3. 通过 Unity MCP 运行测试验证代码正确性:
   - 确保 Unity Editor 已启动并连接 MCP
   - 使用 `run_tests` 工具运行 EditMode 测试
   - 测试未通过则继续排查修复，直到全部通过

### 测试驱动开发（TDD）准则

- **新增功能**：先编写测试覆盖功能场景，再实现功能，最后运行测试验证
- **修复 Bug**：先编写暴露 Bug 的测试，确认测试失败，再修复代码，确认测试通过
- **修改功能**：修改后必须通过 Unity MCP 运行对应模块测试，不通过则继续修复
- **重构**：重构前后必须运行全量测试，确保行为不变

### 测试保留策略

- 项目只保留覆盖**核心功能**的少量测试（当前为渲染图模板/构建冒烟测试）。
- **Agent 使用 TDD 编写的测试均为临时代码**，任务完成后必须删除，不得长期留在仓库。

### MCP 测试运行流程

在功能开发或修改过程中，通过以下步骤确保代码正确性：

1. 确认 Unity Editor 处于运行状态，MCP 已连接
2. 运行 EditMode 测试：

```csharp
// 在 Unity MCP 中执行
vibe_unityMCP_run_tests(mode: "EditMode")
```

3. 查看测试结果：
   - 全部通过 → 继续后续流程
   - 有失败 → 分析失败原因，修复代码，重新运行直到全部通过
4. PR 合并前，运行全量 EditMode 测试确认无回归

### 架构修改

- 回答用户问题时，首先阅读当前项目的架构文档，再给出最合理的解决方案。
- 当用户提出或者和用户一起讨论出新的架构方案时，同步修改架构文档。

---

## 代码规范

### 命名规范

- 一律使用大小驼峰命名法（PascalCase / camelCase），**不使用** `m_`、`s_`、`k_` 等下划线/前缀式命名：
  - 类型、方法、属性、公共成员 → PascalCase
  - 字段、局部变量、方法入参 → camelCase
- 保留统一语义前缀（如 `Pass`）属于类型命名，不属于字段前缀，不做缩写式命名。

### 注释规范

- 项目代码注释一律使用**中文**（代码关键词、类型名、API 名、Unity 术语除外）。
- 所有对外公共 API 需有 XML 文档注释，内容为中文。
- 标记注释统一使用 `// TODO:`、`// FIXME:`、`// NOTE:` 前缀。

---

## 提交规范

- `feat:` — 新功能
- `fix:` — Bug 修复
- `docs:` — 文档更新
- `refactor:` — 代码重构
- `chore:` — 构建/工具变更

---

## 占位说明

项目中标记为 🚧 Stub 的模块目录已创建，但只包含骨架类。待后续完善。
